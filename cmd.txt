gcloud logging read \
  'resource.type="cloud_run_job" AND resource.labels.job_name="oracle-client-image-build"' \
  --project=ggn-nmfs-winde-dev-1 \
  --limit=100 \
  --format="value(textPayload)" | grep -i -E "winrm|iap|tunnel|error|attempt" | head -40
