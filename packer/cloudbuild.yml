# Prerequisites (run once in CLI before first pipeline execution):
#   gcloud services enable osconfig.googleapis.com containeranalysis.googleapis.com
#   gsutil mb -p big-mender-473219-r2 -l us-east4 -b on gs://osconfig-scan-results-big-mender-473219-r2
#   gsutil iam ch serviceAccount:packer-win-sa@big-mender-473219-r2.iam.gserviceaccount.com:roles/storage.objectAdmin gs://osconfig-scan-results-big-mender-473219-r2
#   gcloud projects add-iam-policy-binding big-mender-473219-r2 --member=serviceAccount:packer-win-sa@big-mender-473219-r2.iam.gserviceaccount.com --role=roles/osconfig.vulnerabilityReportViewer
#   gcloud projects add-iam-policy-binding big-mender-473219-r2 --member=serviceAccount:packer-win-sa@big-mender-473219-r2.iam.gserviceaccount.com --role=roles/compute.instanceAdmin.v1
#   gcloud iam service-accounts add-iam-policy-binding packer-win-sa@big-mender-473219-r2.iam.gserviceaccount.com --member=serviceAccount:packer-win-sa@big-mender-473219-r2.iam.gserviceaccount.com --role=roles/iam.serviceAccountUser

substitutions:
  _PROJECT_ID: "big-mender-473219-r2"
  _SOURCE_IMAGE_PROJECT_ID: "windows-cloud"
  _SOURCE_IMAGE_FAMILY: "windows-2022"
  _ZONE: "us-east4-b"
  _IMAGE_FAMILY: "pww-windows-2022-hardened"
  _SOURCE_IMAGE: "ws2022"
  _WINRM_SECRET: "packer-winrm-password"
  _PACKER_TEMPLATE: "harden_ww.pkr.hcl"
  _PACKER_VERSION: "1.9.4"
  _HARDENING_SOURCE_DIR: "./scripts/windows/"
  _HARDENING_TARGET_DIR: "C:/Users/packer_user/hardening/"
  _MACHINE_TYPE: "e2-standard-8"
  _SCAN_MACHINE_TYPE: "e2-medium"
  # NOTE: Cloud Build cannot expand substitutions within the substitutions
  # block. Bucket name must be fully hardcoded — no ${_PROJECT_ID} here.
  _SCAN_RESULTS_BUCKET: "osconfig-scan-results-big-mender-473219-r2"

steps:

# --------------------------------------------------------
# Cost Estimation (Placeholder)
# --------------------------------------------------------
- id: 'llm-cost-estimation'
  name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: bash
  args:
  - -c
  - |
      echo "LLM cost estimation: not-provided"
  dir: 'packer'


# --------------------------------------------------------
# Packer Build
# --------------------------------------------------------
- id: 'packer-build'
  name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: bash
  args:
  - -c
  - |
      set -euo pipefail

      echo "Installing dependencies..."
      apt-get update -y
      apt-get install -y unzip curl jq

      echo "Installing Packer ${_PACKER_VERSION}..."
      curl -fSL \
        "https://releases.hashicorp.com/packer/${_PACKER_VERSION}/packer_${_PACKER_VERSION}_linux_amd64.zip" \
        -o packer.zip
      unzip packer.zip
      mv packer /usr/local/bin/
      rm -f packer.zip
      packer version

      echo "Fetching WinRM password from Secret Manager..."
      PACKER_PW=$$(gcloud secrets versions access latest \
        --project "${_PROJECT_ID}" \
        --secret "${_WINRM_SECRET}")

      export PACKER_LOG=1
      export PACKER_PW="$$PACKER_PW"

      echo "Initializing Packer..."
      packer init "${_PACKER_TEMPLATE}"

      echo "Validating Packer template..."
      packer validate \
        -var "project_id=${_PROJECT_ID}" \
        -var "source_image_project_id=${_SOURCE_IMAGE_PROJECT_ID}" \
        -var "source_image_family=${_SOURCE_IMAGE_FAMILY}" \
        -var "service_account_email=packer-win-sa@${_PROJECT_ID}.iam.gserviceaccount.com" \
        -var "image_family=${_IMAGE_FAMILY}" \
        -var "machine_type=${_MACHINE_TYPE}" \
        -var "zone=${_ZONE}" \
        -var "hardening_source_dir=${_HARDENING_SOURCE_DIR}" \
        -var "hardening_target_dir=${_HARDENING_TARGET_DIR}" \
        -var "hardening_entry_script=run_all.ps1" \
        "${_PACKER_TEMPLATE}"

      echo "Building hardened image..."
      packer build \
        -var "project_id=${_PROJECT_ID}" \
        -var "source_image_project_id=${_SOURCE_IMAGE_PROJECT_ID}" \
        -var "source_image_family=${_SOURCE_IMAGE_FAMILY}" \
        -var "service_account_email=packer-win-sa@${_PROJECT_ID}.iam.gserviceaccount.com" \
        -var "image_family=${_IMAGE_FAMILY}" \
        -var "machine_type=${_MACHINE_TYPE}" \
        -var "zone=${_ZONE}" \
        -var "hardening_source_dir=${_HARDENING_SOURCE_DIR}" \
        -var "hardening_target_dir=${_HARDENING_TARGET_DIR}" \
        -var "hardening_entry_script=run_all.ps1" \
        "${_PACKER_TEMPLATE}"

      echo "Packer build completed successfully."

      LATEST_IMAGE=$$(gcloud compute images list \
        --project="${_PROJECT_ID}" \
        --filter="family=${_IMAGE_FAMILY}" \
        --sort-by="~creationTimestamp" \
        --format="value(name)" \
        --limit=1)
      echo "$$LATEST_IMAGE" > /workspace/latest_image.txt
      echo "Image name saved: $$LATEST_IMAGE"
  dir: 'packer'


# --------------------------------------------------------
# OS Config Vulnerability Scan
#
# HOW IT WORKS:
#   1. Boot a temporary e2-medium VM from the newly built image.
#      OS Config agent starts automatically — it is pre-installed
#      on all GCP Windows Server 2022 images and survives hardening.
#   2. Poll the OS Config API every 30s until the vulnerability
#      report is available (typically 5-10 minutes after boot).
#   3. Fetch the report, upload to GCS for 90-day audit retention.
#   4. Parse for HIGH/CRITICAL CVEs — fail build if any found.
#   5. Delete the scan VM.
#
# NOTE: No heredocs are used in this step intentionally.
# Heredocs (<<EOF) inside YAML multi-line strings cause YAML
# parse errors due to quote and colon conflicts. All multi-line
# content is written using printf or echo -e instead.
# --------------------------------------------------------
- id: 'osconfig-vulnerability-scan'
  name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: bash
  args:
  - -c
  - |
      set -euo pipefail

      LATEST_IMAGE=$$(cat /workspace/latest_image.txt)
      SCAN_VM="osconfig-scan-$$LATEST_IMAGE"
      REPORT_PATH="gs://${_SCAN_RESULTS_BUCKET}/${BUILD_ID}/vulnerability-report.json"

      echo "=== Ensuring GCS audit results bucket exists ==="
      gsutil ls gs://${_SCAN_RESULTS_BUCKET} 2>/dev/null || \
        gsutil mb -p ${_PROJECT_ID} -l us-east4 -b on gs://${_SCAN_RESULTS_BUCKET}

      echo "=== Setting 90-day lifecycle on bucket ==="
      printf '{"rule":[{"action":{"type":"Delete"},"condition":{"age":90}}]}' \
        > /tmp/lifecycle.json
      gsutil lifecycle set /tmp/lifecycle.json gs://${_SCAN_RESULTS_BUCKET}

      echo "=== Booting scan VM from image: $$LATEST_IMAGE ==="
      gcloud compute instances create "$$SCAN_VM" \
        --project="${_PROJECT_ID}" \
        --zone="${_ZONE}" \
        --machine-type="${_SCAN_MACHINE_TYPE}" \
        --image="$$LATEST_IMAGE" \
        --image-project="${_PROJECT_ID}" \
        --service-account="packer-win-sa@${_PROJECT_ID}.iam.gserviceaccount.com" \
        --scopes="cloud-platform" \
        --no-address \
        --boot-disk-size=250GB \
        --boot-disk-type=pd-balanced \
        --metadata="enable-osconfig=TRUE,osconfig-log-level=debug"

      echo "=== Polling OS Config API for vulnerability report (max 15 min) ==="
      REPORT_FOUND=false
      for i in $$(seq 1 30); do
        echo "  [$$i/30] Checking for vulnerability report..."

        REPORT=$$(gcloud compute os-config vulnerability-reports describe "$$SCAN_VM" \
          --project="${_PROJECT_ID}" \
          --zone="${_ZONE}" \
          --format=json 2>/dev/null || echo "")

        if [ -n "$$REPORT" ] && [ "$$REPORT" != "null" ] && [ "$$REPORT" != "" ]; then
          echo "  Report received."
          echo "$$REPORT" > /workspace/vulnerability-report.json
          REPORT_FOUND=true
          break
        fi

        if [ "$$i" -eq 30 ]; then
          echo "ERROR: No vulnerability report within 15 minutes."
          echo "  Check: osconfig.googleapis.com API is enabled"
          echo "  Check: OS Config agent not disabled by STIG policy"
          gcloud compute instances delete "$$SCAN_VM" \
            --project="${_PROJECT_ID}" --zone="${_ZONE}" --quiet || true
          exit 1
        fi

        sleep 30
      done

      echo "=== Deleting scan VM ==="
      gcloud compute instances delete "$$SCAN_VM" \
        --project="${_PROJECT_ID}" \
        --zone="${_ZONE}" \
        --quiet

      echo "=== Uploading report to GCS for audit records ==="
      gsutil cp /workspace/vulnerability-report.json "$$REPORT_PATH"
      echo "Report saved: $$REPORT_PATH"

      echo ""
      echo "=== Parsing vulnerability report ==="

      # Write Python parser to file using printf to avoid YAML heredoc conflicts.
      # Heredocs (<<EOF) inside YAML multi-line strings break the YAML parser
      # when the heredoc body contains colons or quotes — so we use printf instead.
      printf '%s\n' \
        'import json, sys' \
        'with open("/workspace/vulnerability-report.json") as f:' \
        '    report = json.load(f)' \
        'vulnerabilities = report.get("vulnerabilities", {})' \
        'high_critical = []' \
        'for cve_id, details in vulnerabilities.items():' \
        '    detail_list = details.get("details", [])' \
        '    severity = detail_list[0].get("severity", "UNKNOWN") if detail_list else "UNKNOWN"' \
        '    if severity in ("HIGH", "CRITICAL"):' \
        '        pkg = details.get("installedInventoryItemIds", ["unknown"])[0]' \
        '        high_critical.append((severity, cve_id, pkg))' \
        'if not high_critical:' \
        '    print("No HIGH/CRITICAL vulnerabilities found. Image is clean.")' \
        '    sys.exit(0)' \
        'else:' \
        '    print(f"Found {len(high_critical)} HIGH/CRITICAL vulnerabilities:")' \
        '    print()' \
        '    for severity, cve_id, pkg in sorted(high_critical, key=lambda x: x[0]):' \
        '        print(f"  [{severity}] {cve_id}  package: {pkg}")' \
        '    sys.exit(1)' \
        > /tmp/parse_report.py

      python3 /tmp/parse_report.py

      echo ""
      echo "=== OS Config scan PASSED. Image is clean and ready for promotion. ==="


# --------------------------------------------------------
# Deprecate Older Images (Keep Latest Active)
# Only runs if OS Config scan passed — exit 1 above
# prevents promotion of any vulnerable image.
# --------------------------------------------------------
- id: 'deprecate-old-images'
  name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: bash
  args:
  - -c
  - |
      set -euo pipefail

      echo "Retrieving newest image in family ${_IMAGE_FAMILY}..."

      LATEST_IMAGE=$$(gcloud compute images list \
        --project="${_PROJECT_ID}" \
        --filter="family=${_IMAGE_FAMILY}" \
        --sort-by="~creationTimestamp" \
        --format="value(name)" \
        --limit=1)

      if [ -z "$$LATEST_IMAGE" ]; then
        echo "No images found in family."
        exit 0
      fi

      echo "Latest image is: $$LATEST_IMAGE"

      OLD_IMAGES=$$(gcloud compute images list \
        --project="${_PROJECT_ID}" \
        --filter="family=${_IMAGE_FAMILY} AND name!=$$LATEST_IMAGE" \
        --format="value(name)")

      if [ -z "$$OLD_IMAGES" ]; then
        echo "No older images to deprecate."
        exit 0
      fi

      for IMAGE in $$OLD_IMAGES; do
        echo "Deprecating image: $$IMAGE"
        gcloud compute images deprecate "$$IMAGE" \
          --project="${_PROJECT_ID}" \
          --state=DEPRECATED \
          --replacement="$$LATEST_IMAGE"
      done

      echo "Old images deprecated successfully."


# --------------------------------------------------------
# Service Account
# --------------------------------------------------------
serviceAccount: "packer-win-sa@${_PROJECT_ID}.iam.gserviceaccount.com"

options:
  logging: CLOUD_LOGGING_ONLY
